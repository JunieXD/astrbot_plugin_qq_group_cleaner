# 更新记录

## v0.2.3 — 2026-09-21

- 完整记录每次检查、逐人成员筛选及得分排名、命令处理、随机等待、执行前复核、提交与离群核验；重复事件不再合并，原因不再截断。
- 为检查、计划、接口请求和清理操作提供关联编号；补齐取消、未知结果、恢复、数据库错误、接口返回码和异常因果链/调用位置，认证信息脱敏。
- 逐人筛选与活动元数据独立写入screening.log，操作和异常保留在cleaner.log；分别轮转，任一文件失败仍阻止清理，关闭失败也释放其余资源。
- 不新增配置或平台查询，不改变清理条件和执行节奏。

## v0.2.2 — 2026-09-21

- 接口返回的群等级0按有效等级参与筛选、排序和执行前复核，不再仅因0级保留成员，不增加额外资料查询或配置项。
- 未发言天数、管理员、新成员及其他保护继续生效；真正缺失或无效的群等级仍保留，QQ等级0的补查策略不变。
- 补充0级加权评分、排序和执行前等级变化的回归测试，更新配置说明。

## v0.2.1 — 2026-09-21

- 普通启动/重载改为默认30～90秒短缓冲，连接或异常恢复保留默认300～900秒冷却；两组范围均可在执行节奏中配置。
- 等待日志、状态和恢复回复区分缓冲、恢复冷却及批次间隔，显示北京时间截止时间。
- 保存读取阶段观察到的断线/异常冷却与离线状态，短重载不能绕过已记录的恢复等待；已有截止时间、批次间隔、操作预约和额度不提前或清零。
- 已配置的恢复范围同步用于管理员恢复、平台连接恢复和共享操作队列；异常暂停仍需核对处理。

## v0.2.0 — 2026-09-21

- “综合排序”改为可配置的加权评分，默认未发言时长70、群等级30、QQ等级0；得分越高越优先。
- 每群独立设置权重，自动按比例计算，0表示不参与；默认省去为排名补查候选QQ等级的请求。
- 保留全部保护条件和独立等级上限，开启维度缺失仍保留；修改权重会使旧计划失效。
- 预览和解释显示得分，资料未齐时明确“已核验人数”，避免将补查中的成员误认为不符合条件。
- 旧的综合顺序仍可用自定义排序复现，升级说明与公式见配置文档。

## v0.1.2 — 2026-09-21

- 修复查询期间配置切换使旧群规则/机器人绑定混入新计划的问题。
- 处理成员与全员禁言通知，保护证据跨重载保留；解除通知遗漏可通过“恢复”重新核验。
- 两次名单读取合并保护证据，避免第二次旧缓存抹掉第一次查到的近期活动等信息。
- 人工保留与永久保护改为单一事务，写入失败或取消不会只完成一半。
- 数据库调用在重复取消时仍等待完成，取消期间发生写入错误仍停止清理。
- 统一各 Python 版本的网络任务调度，在传输任务内部执行最后检查，拦截已排队的状态变化。
- 复现条件、测试和保留的接口边界见 [第二轮审查](docs/review-0.1.2.md)。

## v0.1.1 — 2026-09-21

- 修复旧名单复活离群成员、入群时间回退创建新代次，以及近期平台活动被旧数据覆盖的问题。
- 保存QQ等级补查的中间进度，扩展缓存窗口以支持大群；失败成员暂时保留，避免反复从头查询。
- 多群独立调度、持久化分级读取预算，给执行复核和管理员命令留出额度。
- 连接变化、观察到离线恢复和长时间接口排队使旧计划失效；显式绑定QQ身份，隔离已识别的其他账号离线。
- 已收到的撤管理员通知覆盖旧授权缓存；账号异常暂停只允许来源群管理员恢复。
- 强化写前事务复核、跨群未决操作检查，以及初始化取消和关闭异常时的资源释放。
- 新增现实场景回归测试，完整审查见 docs/review-2026-09-21.md。

## v0.1.0 — 2026-09-21

- 首个可安装实现：按群人数触发，支持仅预览、确认后清理和自动清理。
- 每群独立配置未发言时间、新成员保护、等级条件、保护名单和清理顺序。
- 中文私聊预览、确认、状态、暂停、保护、解释、历史与未知结果处理。
- 等待后复核、群与账号额度、SQLite写前意图、结果核验、重载恢复和日志轮转。
- 全员禁言保护、NapCat字符串身份参数；修复Windows重复实例检查在读取已锁定字节时提前抛错的问题。
- 核心模块与AstrBot入口分离；提供模拟测试及Windows/Linux持续集成。
- 原方案中复杂条件编辑器、消息条数、导出、主动通知等扩展暂未开放，使用文档已按实际实现重写。
